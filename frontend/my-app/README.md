# 🧠 Flashcards App — React + Flask

This is a full-stack flashcards application with a **React frontend** and a **Flask backend**. It pulls flashcards from a CSV file and allows users to browse cards by category to study.

---

## 🚀 Set Up

cd backend
pip install flask flask-cors pandas
python app.py

cd ./frontend
npm install
npm run dev


